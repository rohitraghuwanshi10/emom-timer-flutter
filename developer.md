# Developer Guidelines

* Do not build the distribution (`dist`) folder or release binaries automatically. Build compilation should only be run to verify code correctness, or if explicitly requested.
